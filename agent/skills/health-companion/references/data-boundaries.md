# Health Agent 数据边界

- 摘要只包含饮食聚合、步数、训练次数与分钟、睡眠、体重和心情的最小必要信息。
- 趋势只开放体重、睡眠和步数的 7–90 天聚合统计，不返回逐日原始序列。
- 草案类型仅为 `metric`、`meal`、`workout`，状态固定为 `pending`，不会直接写入正式记录。
- 血压、血氧、化验原始值、正式批量修正和对外导出不进入普通 `shadow-health` Profile。
