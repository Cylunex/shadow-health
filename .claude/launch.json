{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "shadow-health",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8801} --reload"],
      "port": 8801,
      "autoPort": true
    }
  ]
}
