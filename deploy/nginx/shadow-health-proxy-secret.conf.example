# 复制为 ECS /etc/nginx/snippets/shadow-health-proxy-secret.conf，权限 root:root 0600。
# 生成：openssl rand -base64 48
# 同一明文写入 NAS 的 SHADOW_PROXY_AUTH_SECRET_FILE；不得提交真实值。
proxy_set_header X-Shadow-Proxy-Secret "REPLACE_WITH_RANDOM_PROXY_SECRET";
